# ============================================
# RVCBotBuilds - Morphe (arm64-v8a)
# With Auto-Detach (Zygisk) + Custom Patches
# ============================================

enable-module-update = true
parallel-jobs = 1
compression-level = 9
riplib = true
remove-rv-integrations-checks = true
continue-on-error = true

# === Architecture ===
default-arch = "arm64-v8a"

# ============================================
# YouTube Morphe - APK + Module (arm64-v8a)
# AMOLED dark theme (pure black)
# Auto-Detach blocks Play Store updates
# ============================================
[YouTube-Morphe]
enabled = true
app-name = "YouTube"
patches-source = "MorpheApp/morphe-patches"
patches-version = "latest"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "both"
arch = "arm64-v8a"
included-patches = "'Theme'"
excluded-patches = "'Custom branding'"
patcher-args = "-OdarkThemeBackgroundColor=@android:color/black"
uptodown-dlurl = "https://youtube.en.uptodown.com/android"
apkmirror-dlurl = "https://www.apkmirror.com/apk/google-inc/youtube"

# ============================================
# Music Morphe - APK + Module (arm64-v8a)
# AMOLED dark theme (pure black)
# ============================================
[Music-Morphe]
enabled = true
app-name = "Music"
patches-source = "MorpheApp/morphe-patches"
patches-version = "latest"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "both"
arch = "arm64-v8a"
included-patches = "'Theme'"
excluded-patches = "'Custom branding'"
patcher-args = "-OdarkThemeBackgroundColor=@android:color/black"
uptodown-dlurl = "https://youtube-music.en.uptodown.com/android"
apkmirror-dlurl = "https://www.apkmirror.com/apk/google-inc/youtube-music"

# ============================================
# Instagram Piko - APK (arm64-v8a)
# crimera/piko v3.5.0-dev: 21 patches + FLAG_SECURE custom
#
# Base: 430.0.0.53.80 (piko recommended version)
# Patches: dev channel (latest fixes for Download media, ghost mode, etc.)
#
# 🖤 AMOLED theme (pure black)
# 📥 Download: posts, reels, stories, highlights, profile pics, audio, voice messages
# 🚫 Ads: remove all ads + suggested content
# 📸 Screenshots: FLAG_SECURE removed + screenshot detection disabled
# 🔧 Settings: Piko Settings hub + Developer options
# 🛡️ Privacy: analytics off, typing hidden, links sanitized
# 🧹 Clean: hide stories tray, nav buttons, build popup
#
# REMOVED (risky/detectable/unnecessary):
#   View DMs anonymously — Instagram can detect ghost mode in DMs (ban risk)
#   View live anonymously — Same detection risk
#   View stories anonymously — High detection risk, Meta actively monitors
#   View story mentions — Cascading dependency, buggy
#   Limit feed to following profiles — Breaks feed algorithm, missing content
#   Make ephemeral media permanent — View-once messages staying = detection risk
#   Follow back indicator — Minor, unreliable across versions
#   Hide navigation buttons — Can break UI, confusing UX
#   More options on profile — Buggy on many versions
#   Sanitize share links — Low value (just removes tracking params)
#
# EXCLUDED (dangerous):
#   Unlock employee options — Exposes internal Meta tools
#   Unlock Plus benefits — TOS violation, detectable
# ============================================
[Instagram-Piko]
app-name = "Instagram"
patches-source = "crimera/piko"
patches-version = "dev"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "apk"
version = "430.0.0.53.80"
exclusive-patches = true
patcher-args = "--continue-on-error"
arch = "arm64-v8a"
included-patches = """\
  'Add settings' \
  'Amoled theme' \
  'Copy comment' \
  'Disable analytics' \
  'Disable ads' \
  'Disable screenshot detection' \
  'Disable typing status' \
  'Download media' \
  'Hide stories tray' \
  'Hide suggested content' \
  'Improve image viewing' \
  'More options on post' \
  'Open links externally' \
  'Remove build expired popup' \
  'Sanitize share links' \
  'Unlock developer options' \
  """
excluded-patches = """\
  'Unlock employee options' \
  'Unlock Plus benefits' \
  'View DMs anonymously' \
  'View live anonymously' \
  'View stories anonymously' \
  'View story mentions' \
  'Limit feed to following profiles' \
  'Make ephemeral media permanent' \
  'Follow back indicator' \
  'Hide navigation buttons' \
  'More options on profile' \
  """
# Custom Smali patch applied AFTER ReVanced CLI:
#   📸 Allow Screenshots in DMs — removes FLAG_SECURE from Window.setFlags
# See: custom-patches/apply-custom-patches.sh
uptodown-dlurl = "https://instagram.en.uptodown.com/android"
