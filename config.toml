# ============================================
# RVCBotBuilds — Instagram Mod
# arm64-v8a | Morphe patches | Piko fork
# ============================================

enable-module-update = true
parallel-jobs = 2
compression-level = 9
riplib = true
remove-rv-integrations-checks = true
continue-on-error = false
default-arch = "arm64-v8a"

# ============================================
# YouTube Morphe — APK + Magisk Module
# ============================================
[YouTube-Morphe]
enabled = true
app-name = "YouTube"
patches-source = "MorpheApp/morphe-patches"
patches-version = "latest"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "both"
arch = "arm64-v8a"
included-patches = "'Theme'"
excluded-patches = "'Custom branding' 'SponsorBlock' 'Return YouTube Dislike' 'Hide player overlay buttons'"
patcher-args = "-OdarkThemeBackgroundColor=@android:color/black"
uptodown-dlurl = "https://youtube.en.uptodown.com/android"
apkmirror-dlurl = "https://www.apkmirror.com/apk/google-inc/youtube"

# ============================================
# Music Morphe — APK only
# ============================================
[Music-Morphe]
enabled = true
app-name = "Music"
patches-source = "MorpheApp/morphe-patches"
patches-version = "latest"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "apk"
arch = "arm64-v8a"
included-patches = "'Theme'"
excluded-patches = "'Custom branding' 'Enable debug logging' 'SponsorBlock'"
patcher-args = "-OdarkThemeBackgroundColor=@android:color/black"
uptodown-dlurl = "https://youtube-music.en.uptodown.com/android"
apkmirror-dlurl = "https://www.apkmirror.com/apk/google-inc/youtube-music"

# ============================================
# Instagram Mod — Chrispsz/piko fork
#
# IMPORTANT: version = "auto" lets the CLI pick the
# Instagram version supported by the .mpp. This prevents
# the version mismatch bug where piko v3.4.0 (supports 426)
# silently skips ALL patches for Instagram 430.
#
# If you want a specific version, change to e.g.:
#   version = "430.0.0.53.80"
# But make sure your fork's .mpp supports that version!
#
# Custom modifications (immutable overlay):
#   🔧 HookFlags: patch flags only (no JSON — use Instagram importer for extras)
#   🔄 OTA: in-app APK updater from GitHub
#   🔍 Debug: ADB DebugReceiver (always-on, no toggle needed)
#   🏷️ Rebrand: Piko → Mod (all strings)
#   🇧🇷 PT-BR translation complete
#   🖤 AMOLED theme
#   📥 Download media
#   🛡️ Privacy patches
#   🔗 Link sanitization
# ============================================
[Instagram-Piko]
app-name = "Instagram"
patches-source = "Chrispsz/piko"
patches-version = "dev"
cli-source = "MorpheApp/morphe-cli"
rv-brand = "Morphe"
build-mode = "apk"
version = "auto"
exclusive-patches = true
arch = "arm64-v8a"
included-patches = """\
  'Add settings' \
  'Amoled theme' \
  'Copy comment' \
  'Disable analytics' \
  'Disable ads' \
  'Disable screenshot detection' \
  'Disable typing status' \
  'Download media' \
  'Follow back indicator' \
  'Hide suggested content' \
  'Improve image viewing' \
  'More options on post' \
  'More options on profile' \
  'Open links externally' \
  'Remove build expired popup' \
  'Sanitize share links' \
  'Unlock developer options' \
  'View stories anonymously' \
  'View DMs anonymously' \
  'Remove empty bottom space' \
  """
excluded-patches = """\
  'Unlock employee options' \
  'Unlock Plus benefits' \
  """
uptodown-dlurl = "https://instagram.en.uptodown.com/android"
