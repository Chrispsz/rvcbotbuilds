name: Build Instagram APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'config.toml'
      - 'utils.sh'
      - 'build.sh'
      - 'custom-patches/**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq zip

      - name: Build Instagram only
        run: |
          chmod +x build.sh utils.sh
          bash build.sh config.toml 2>&1 | tee build.log

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: instagram-patched
          path: build/*.apk
          retention-days: 30

      - name: Upload Modules
        uses: actions/upload-artifact@v4
        with:
          name: magisk-modules
          path: build/*.zip
          retention-days: 30

      - name: Build log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: build.md
          retention-days: 7
