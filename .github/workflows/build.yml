# build.yml: O Trabalhador
# Responsabilidade: Compilar os apps, criar a release, enviar notificação
# para o Telegram e limpar artefatos antigos.

name: Workflow de Build Reutilizável
on:
  workflow_call:
    inputs:
      build_config:
        required: true
        type: string

permissions:
  contents: write
  actions: read

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true
          fetch-depth: 0

      - name: Configurar Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Obter Informações da Última Build
        id: last_build
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LAST_TAG=$(gh release list -L 1 | awk -F '\t' '{print $3}' | grep -oP '[0-9]+' || echo "0")
          NEXT_VER_CODE=$((LAST_TAG + 1))
          # Pega o ID da mensagem do Telegram da release anterior para poder apagá-la
          LAST_MSG_ID=""
          if [[ "$LAST_TAG" != "0" ]]; then
            BODY=$(gh release view "$LAST_TAG" --json body -q '.body' || echo "")
            LAST_MSG_ID=$(echo "$BODY" | grep -oP 'TG_MSG_ID: \K[0-9]+' || echo "")
          fi
          echo "next_ver_code=$NEXT_VER_CODE" >> $GITHUB_OUTPUT
          echo "last_msg_id=$LAST_MSG_ID" >> $GITHUB_OUTPUT

      - name: Compilar Apps
        run: |
          echo "Recebida a configuração de build do job 'check'."
          echo '${{ inputs.build_config }}' > config.json
          echo "Conteúdo de config.json gerado:"
          cat config.json
          echo "Iniciando a compilação..."
          ./build.sh config.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          NEXT_VER_CODE: ${{ steps.last_build.outputs.next_ver_code }}
          
      - name: Preparar Conteúdo da Release e Notificação
        id: content
        run: |
          DELIM="$(openssl rand -hex 8)"
          echo "changelog<<${DELIM}" >> "$GITHUB_OUTPUT"
          cat build.md >> "$GITHUB_OUTPUT"
          echo "${DELIM}" >> "$GITHUB_OUTPUT"

      - name: Enviar Notificação para o Telegram
        id: telegram
        if: success()
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          cd build || { echo "Pasta 'build' não encontrada"; exit 1; }
          NL=$'\n'
          APKS=""
          MODULES=""
          TAG="${{ steps.last_build.outputs.next_ver_code }}"
          BASE_URL="${{ github.server_url }}/${{ github.repository }}/releases/download/${TAG}"
          for OUTPUT in *; do
            if [[ $OUTPUT == *.apk ]]; then APKS+="${NL}📦 [${OUTPUT}](${BASE_URL}/${OUTPUT})";
            elif [[ $OUTPUT == *.zip ]]; then MODULES+="${NL}📦 [${OUTPUT}](${BASE_URL}/${OUTPUT})"; fi
          done
          BODY="${{ steps.content.outputs.changelog }}"
          FORMATTED_BODY=$(echo "$BODY" | sed 's/^\* \*\*/↪ \*\*/g; s/^\* `/↪ \*\*/g; s/`/\*/g; s/^\* /\↪/g; s/\*\*/\*/g; s/###//g; s/^- /↪ /g; /^==/d;')
          MSG="*Build #${TAG} disponível!*${NL}${NL}${FORMATTED_BODY}${NL}${NL}*▼ Downloads*${NL}"
          if [[ -n "$MODULES" ]]; then MSG+="*Módulos:*${MODULES}${NL}"; fi
          if [[ -n "$APKS" ]]; then MSG+="*APKs:*${APKS}${NL}"; fi
          RESPONSE=$(curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" --data-urlencode "chat_id=${TG_CHAT_ID}" --data-urlencode "text=${MSG}" --data-urlencode "parse_mode=Markdown" --data-urlencode "disable_web_page_preview=true")
          MSG_ID=$(echo "$RESPONSE" | grep -oP '"message_id":\K[0-9]+' || echo "")
          [ -n "$MSG_ID" ] && echo "msg_id=$MSG_ID" >> $GITHUB_OUTPUT
          
          # =============================================================
          # PONTO CHAVE: APAGAR A MENSAGEM ANTERIOR DO TELEGRAM
          # Se encontramos um ID de mensagem na release anterior,
          # enviamos um comando para a API do Telegram para apagá-la.
          # =============================================================
          LAST_MSG_ID="${{ steps.last_build.outputs.last_msg_id }}"
          if [[ -n "$LAST_MSG_ID" ]]; then
            echo "Apagando mensagem anterior do Telegram (ID: $LAST_MSG_ID)..."
            curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/deleteMessage" -d "chat_id=${TG_CHAT_ID}" -d "message_id=${LAST_MSG_ID}" >/dev/null 2>&1 || true
          fi

      - name: Criar Release e Enviar Artefatos
        if: success()
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ steps.last_build.outputs.next_ver_code }}
          release_name: "Build #${{ steps.last_build.outputs.next_ver_code }}"
          body: |
            ${{ steps.content.outputs.changelog }}

            ---
            TG_MSG_ID: ${{ steps.telegram.outputs.msg_id }}
          file: ./build/*
          file_glob: true
          overwrite: true
      
      - name: Atualizar Branch de JSON do Magisk
        if: success()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B update
          get_update_json() { echo "{\"version\": \"$1\",\"versionCode\": ${{ steps.last_build.outputs.next_ver_code }},\"zipUrl\": \"$2\",\"changelog\": \"https://raw.githubusercontent.com/$GITHUB_REPOSITORY/update/build.md\"}"; }
          cd build || exit 1
          for OUTPUT in *magisk*.zip; do
            [ "$OUTPUT" = "*magisk*.zip" ] && continue
            ZIP_S=$(unzip -p "$OUTPUT" module.prop)
            if ! UPDATE_JSON=$(echo "$ZIP_S" | grep updateJson); then continue; fi
            UPDATE_JSON="${UPDATE_JSON##*/}"
            VER=$(echo "$ZIP_S" | grep version=)
            VER="${VER##*=}"
            DLURL="${{ github.server_url }}/${{ github.repository }}/releases/download/${{ steps.last_build.outputs.next_ver_code }}/${OUTPUT}"
            get_update_json "$VER" "$DLURL" >"../$UPDATE_JSON"
          done
          cd ..
          git add -f build.md ./*-update.json
          if ! git diff --staged --quiet; then
            git commit -m "chore: Bump para build #${{ steps.last_build.outputs.next_ver_code }}"
            git push origin update --force
          else
            echo "Nenhuma mudança nos arquivos de atualização. Pulando o commit."
          fi
          
      - name: Limpar releases antigas do GitHub
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # =============================================================
          # PONTO CHAVE: APAGAR BUILDS ANTERIORES NO GITHUB
          # Este comando lista todas as releases, pula as 5 mais recentes
          # e apaga todas as outras mais antigas.
          # =============================================================
          echo "Limpando releases antigas, mantendo as 5 mais recentes..."
          gh release list --repo ${{ github.repository }} --limit 100 --json tagName | \
          jq -r '.[5:].[].tagName' | \
          xargs -r -I {} gh release delete {} --repo ${{ github.repository }} --yes --cleanup-tag

      - name: Notificar em Caso de Falha
        if: failure()
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
        run: |
          MSG="❌ *Falha no Build #${{ steps.last_build.outputs.next_ver_code }}*\n\nOcorreu um erro durante a compilação.\n\n[Verificar logs](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
          curl -s -X POST "https://api.telegram.org/bot${TG_TOKEN}/sendMessage" -d "chat_id=${TG_CHAT_ID}" -d "text=${MSG}" -d "parse_mode=Markdown"
